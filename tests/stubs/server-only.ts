// vitest stub: the real 'server-only' package throws outside a React Server
// Component graph. Aliased in vitest.config.ts so that server modules can be
// unit-tested. The build-time guarantee is unaffected — Next still enforces it.
export {};
